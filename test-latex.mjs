import React from 'react'; import Latex from 'react-latex-next'; import ReactDOMServer from 'react-dom/server'; const str = 'const React = require('react'); const Latex = require('react-latex-next'); const ReactDOMServer = require('react-dom/server'); const C = Latex.default || Latex; const str = '$$E_{\\text{Inpainting}}$'; console.log(ReactDOMServer.renderToString(React.createElement(C, null, str)));E_{\\\\text{Inpainting}}$'; console.log(ReactDOMServer.renderToString(React.createElement(Latex, null, str)));
